import React from "react";
//import { useHistory } from 'react-dom';

const AddItem = () => {
  //const history = useHistory();
  return (
    <div>
      <p> Add new Item in the Inventory</p>
    </div>
  );
};

export default AddItem;
